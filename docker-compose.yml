# تشغيل MongoDB محلياً: docker compose up -d
services:
  mongo:
    image: mongo:7
    restart: unless-stopped
    ports:
      - '27017:27017'
    volumes:
      - daytoday_mongo:/data/db

volumes:
  daytoday_mongo:
